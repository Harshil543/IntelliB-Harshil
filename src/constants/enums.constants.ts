export enum MeterType {
  ELECTRICITY = 'electricity',
  WATER = 'water',
  GAS = 'gas',
  DIESEL_GENERATOR = 'diesel_generator'
}

export enum Category {
  FLAT_RATE = 'FLAT_RATE',
  SLAB_WISE_RATE = 'SLAB_WISE_RATE'
}
